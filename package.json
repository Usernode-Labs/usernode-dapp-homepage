{
  "name": "usernode-dapp-homepage",
  "version": "1.0.0",
  "description": "Usernode dapps directory homepage with on-chain stats, per-user pinned favorites, and micro-blog feed.",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js --local-dev"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "pg": "^8.11.5"
  }
}
